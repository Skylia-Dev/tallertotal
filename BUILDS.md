# Historial de builds — TallerTotal

Changelog de cada deploy a producción. Se actualiza a mano en cada build nuevo (mismo criterio que `frontend/next.config.ts`, que define `NEXT_PUBLIC_APP_VERSION` y se muestra en el login).

**Convención**: cada fila es un deploy real a producción (Vercel para el frontend, Railway para el backend — la mayoría de los builds tocan uno de los dos, algunos ambos). El número de build es el orden cronológico de deploys. La versión se incrementa un paso (`v1.X` → `v1.X+1`) en cada deploy nuevo, en `frontend/next.config.ts`.

## Notas operativas

- **2026-09-14**: se empieza a llevar este changelog. Los builds 1 a 19 (migración completa de funcionalidad de FiltCar + WhatsApp Meta + temas + módulos ocultables + rename de MecaFlow→TallerTotal + fixes) ya estaban deployados bajo la misma versión `v1.50` (no se incrementaba con cada deploy todavía). Desde el build 20 se bump la versión en cada deploy.
- **2026-09-14**: se detectó que el auto-deploy de Railway y Vercel estaba roto desde hacía meses — el repo se había transferido de `julianmarmol1979/tallertotal` a la org `Skylia-Dev/tallertotal` en GitHub y la conexión Git quedó rota en ambos paneles. Railway además tenía el Root Directory apuntando a una carpeta que no existía todavía (`/backend/TallerTotal.Api`, cuando la carpeta real seguía llamándose `MecaFlow.Api`). Se resolvió renombrando la carpeta y reconectando Git en ambos paneles (disconnect + reconnect completo). Confirmado funcionando con pushes de prueba (builds 15-17).

## Builds

| # | Fecha | Commit | Versión | Qué se hizo |
|---|-------|--------|---------|-------------|
| 47 | 2026-09-25 | — | v1.78 | Fix: el botón de la Libreta Digital decía "Descargar PDF" pero solo abre el diálogo de impresión del navegador (el usuario todavía tiene que elegir "Guardar" ahí) — se renombra a "Imprimir / Guardar PDF" para no confundir, igual que ya decía el botón equivalente en la impresión de órdenes |
| 46 | 2026-09-25 | — | v1.77 | Libreta Digital: botón "Descargar PDF" (usa el diálogo nativo de impresión del navegador, sin librerías nuevas) y rediseño visual — portada con degradé azul del taller + patente/titular en una sola tarjeta tipo carnet, barra de color según urgencia (rojo/ámbar/verde) en el próximo cambio, timeline más prolijo. Estilos `print:` para que se vea bien tanto en pantalla como en el PDF exportado |
| 45 | 2026-09-25 | — | v1.76 | Rename de "Placa" a "Patente" en toda la UI (labels, placeholders, encabezados de tabla, export a Excel) — nomenclatura más usada en Argentina. Se cargó historial de demo (3 services de Lubricentro) al vehículo EF456GH para poder mostrar la Libreta Digital ya poblada |
| 44 | 2026-09-25 | — | v1.75 | Libreta Digital del vehículo (link/QR público con el historial de services): cada vehículo tiene un token público estable (`Vehicle.PortalToken`), expuesto en `/libreta/[token]` — muestra el taller, el próximo cambio de aceite (con estado Vencido/Próximo/Al día calculado por km y fecha) y el timeline completo de services de Lubricentro (aceite, filtros, km, notas). En Vehículos hay un botón nuevo que abre un diálogo con el código QR (vía `qrcode`), el link copiable y un botón directo a WhatsApp del cliente. El botón de "Les toca volver" en Vencimientos ahora manda ese link en el mensaje. Se agregó `Tenant.Phone` (configurable en Configuración → Integraciones) para el botón "Pedir turno" de la libreta pública |
| 43 | 2026-09-16 | — | v1.74 | Fix: la pantalla de login se rompía visualmente (labels y asterisco "Usuario *"/"Contraseña *" casi invisibles, inputs sin contorno) cuando el navegador tenía guardado el modo Oscuro/Tenue de una sesión anterior — el login usa fondo blanco fijo pero heredaba las variables de color del modo oscuro porque el `<html>` aplica esa clase globalmente antes del login. Se fuerza la pantalla de login a modo claro siempre (clase `.force-light` en `globals.css`), sin importar el tema guardado |
| 42 | 2026-09-16 | — | v1.73 | Sistema de menú configurable por rol: en Configuración → Módulos ahora hay una matriz módulo × rol (Dueño / Administrador / Empleado / SuperAdmin) donde se desmarca qué ve cada rol — reemplaza la lista única anterior que ocultaba un módulo para todo el taller por igual. Mecánico sigue con su propio menú fijo (Dashboard + Mis Órdenes), no se toca acá. En el backend, `Tenant.HiddenModulesJson` (un solo valor por taller) se reemplaza por una tabla `RoleModuleConfigs` (un valor por rol). Segunda tanda de fixes de contraste en modo Oscuro/Tenue: barrido de ~300 clases de color hardcodeadas (`bg-white`, `text-gray-*`, `border-gray-*`) a los tokens de tema en las ~30 páginas y componentes restantes de la app — incluye el bug puntual reportado (encabezado de la tabla de Deudas invisible en Oscuro/Tenue) y el mismo problema en Órdenes, Clientes, Artículos, etc. También se corrige el fondo de página en modo Claro (antes `--card` y `--background` eran el mismo blanco, sin separación visual entre tarjetas y fondo) y los gráficos del Dashboard (ejes, grilla y tooltip de Recharts, que usaban colores fijos en vez de las variables del tema) |
| 41 | 2026-09-16 | — | v1.72 | Dos colores más en la paleta de Apariencia: "Verde claro" (lima, para distinguirlo bien del "Verde" existente) e "Índigo". El picker de colores pasa a grilla de 5 columnas (2 filas de 5 en vez de 3 filas desparejas). Fix: la barra lateral izquierda era siempre negra sin importar el modo elegido — ahora usa las mismas variables de tema que el resto de la app (`--sidebar`, `--sidebar-foreground`, etc.), así que en Claro se pone blanca, en Oscuro gris oscuro y en Tenue casi negro cálido, igual que el resto de la interfaz |
| 40 | 2026-09-16 | — | v1.71 | El mecánico asignado a una orden ahora se filtra por especialidad según el tipo: en "Cambio de aceite" solo aparecen mecánicos con especialidad "Lubricentro" o la nueva "General + Lubricentro" (agregada al catálogo de especialidades); en "Reparación general" aparecen todos menos los de "Lubricentro" puro. Antes cualquier mecánico podía quedar asignado a cualquier tipo de orden sin importar su especialidad. Si se cambia el tipo de orden y el mecánico ya seleccionado deja de calificar, se deselecciona automáticamente. Aplica tanto a "Nueva orden" como a "Editar orden" |
| 39 | 2026-09-16 | — | v1.70 | Fix: el badge de versión junto al logo (agregado en v1.69) quedaba pisando el texto "TallerTotal" en la barra lateral — se pasa a una línea propia debajo del logo. Se agrega el modo "Tenue" (gris grafito cálido, intermedio entre Claro y Oscuro) al selector de Apariencia, igual que en CEMDI: tercer botón en "Modo" (ícono Contrast), variantes por color de acento, y clase `.tenue` en el CSS aplicada igual que `.dark` (incluye el script de precarga para evitar flash al recargar) |
| 38 | 2026-09-16 | — | v1.69 | Reordenamiento de la identidad visual del header: la versión de la app se saca del pie de la barra lateral (quedaba escondida) y se pone arriba a la izquierda, junto al logo "TallerTotal". En la topbar aparece ahora "Usuario: {username}" a la izquierda (antes decía "Taller: {nombre}" en la barra lateral, poco útil). El avatar de perfil suma una insignia de cámara azul siempre visible (antes solo se entendía que era clickeable al pasar el mouse) para que quede claro que ahí se cambia la foto |
| 37 | 2026-09-16 | — | v1.68 | Especialidad de mecánico pasa de texto libre a un dropdown con las especialidades típicas de un taller (motor, electricidad, frenos, chapa y pintura, etc.) + "Otro" al final para lo que no está en la lista, con un campo de texto que se habilita solo en ese caso. Aplica tanto al alta de usuario como a "Editar mecánico" |
| 36 | 2026-09-16 | — | v1.67 | Fix: la versión al pie de la barra lateral se veía demasiado chica (10px, gris muy oscuro sobre el fondo oscuro). Se agranda a texto normal y color más claro para que se lea bien |
| 35 | 2026-09-16 | — | v1.66 | Se muestra la versión de la app (`v1.66`) al pie de la barra lateral, debajo de "Usuarios". El botón "Agregar tarjeta" del dashboard personalizable se mueve de abajo del todo (fácil de perder de vista) a arriba junto a "Personalizar"/"Listo", como botón sólido bien visible |
| 34 | 2026-09-16 | — | v1.65 | Dashboard personalizable (igual que en CEMDI): botón "Personalizar" arriba a la derecha activa un modo edición donde cada tarjeta se puede ocultar (X) o arrastrar para reordenar, y "Agregar tarjeta" permite volver a sumar las que se sacaron. El layout se guarda por usuario (no por taller), como JSON en `Users.DashboardLayout` — si nunca lo tocás, ves el layout de siempre (5 KPIs + 4 gráficos). Catálogo ampliado con 7 estadísticas nuevas que antes no aparecían en ningún lado del dashboard: Ventas del mes (POS, separado de ingresos por órdenes), Deudas pendientes, Presupuestos vigentes, Balance de caja, Artículos con stock bajo, Vencimientos de lubricentro (próx. 30 días) y Clientes nuevos del mes. Implementado con `@dnd-kit` (drag-and-drop), probado en producción: ocultar, agregar y arrastrar para reordenar, los tres persisten correctamente |
| 33 | 2026-09-15 | — | v1.64 | Cambiar contraseña y foto de perfil (igual que en CEMDI): nuevo botón "Contraseña" en la topbar (contraseña actual + nueva + confirmar). La foto se sube haciendo clic directo en el círculo de avatar de la topbar (sin diálogo, como en CEMDI) — se guarda como `bytea` en la propia base (no en Supabase Storage, para no depender de que el taller tenga eso configurado), máximo 5MB, solo `image/*`. De paso encontré y arreglé un bug real en el proxy genérico (`/api/proxy/[...path]`): usaba `.text()` en ambas direcciones, lo que corrompe cualquier contenido binario (imágenes, y también el PDF de Agenda, que tenía este mismo problema desde que existe) — ahora usa `arrayBuffer()` y preserva el Content-Type real de la petición en vez de pisarlo siempre con `application/json`. Verificado con bytes idénticos antes/después de subir y descargar una imagen real a través del proxy |
| 32 | 2026-09-15 | — | v1.63 | Cierre de sesión por inactividad, configurable (igual que en CEMDI): nueva tarjeta "Cierre de sesión por inactividad" en Configuración → Integraciones, minutos editables (1-1440, default 30). Se aplica en vivo contra la base en cada request (`InactivitySessionMiddleware`) — no depende de la expiración propia del JWT (que sigue siendo un techo duro de 7 días) — así que bajar el valor puede cortar sesiones ya activas al toque. El frontend manda un heartbeat cada minuto mientras hay actividad real (mouse/teclado/scroll) y además desloguea proactivo del lado del cliente si nadie tocó nada en el tiempo configurado. De paso: cualquier 401 de la API ahora redirige solo al login en vez de dejar errores sueltos por pantalla |
| 31 | 2026-09-15 | — | v1.62 | Órdenes de Lubricentro: se agrega un "Tipo de orden" (General / Lubricentro, pensado para sumar más tipos a futuro — gomería, chapa y pintura, etc.) al mismo módulo de Órdenes, sin duplicar portal/WhatsApp/exportación/historial. Al elegir Lubricentro aparecen los campos propios del cambio de aceite (marca, tipo/viscosidad, litros, filtros cambiados, próximo cambio por km y/o fecha con sugerencia automática de +10.000km/+6 meses) y un checklist de inspección tri-estado (Sí/No/Sin revisar) que se auto-adjunta según el tipo de orden. Nueva pantalla "Vencimientos" (`/ordenes/vencimientos`) que lista vehículos con el cambio próximo a vencer o vencido, calculado contra el último kilometraje conocido, con acceso directo a WhatsApp. Recordatorio automático por WhatsApp cuando se acerca la fecha del próximo cambio (mismo mecanismo que el aviso de "orden inactiva"). Investigamos FiltCar buscando replicar su lógica de lubricentro y resultó que no tenía nada de esto implementado — se diseñó de cero, reutilizando de FiltCar solo el patrón de checklist auto-adjuntado. De paso: bug de encoding en Windows — un archivo .cs nuevo con texto en español sin BOM UTF-8 hacía que el compilador leyera mal los acentos (se ve en runtime, no en el editor); los archivos nuevos con texto en español ahora llevan BOM |
| 30 | 2026-09-15 | — | v1.61 | Autenticación de dos factores (TOTP, igual que en CEMDI): cada usuario la activa desde "Seguridad" en la barra de arriba — QR con Google Authenticator/Authy, código de 6 dígitos para confirmar. El login pasa a dos pasos cuando está activa (ticket temporal de 5 min + código). Se pobló de datos de demo toda tabla que estaba vacía (proveedores, artículos, compras, ventas, presupuestos, caja, auditoría, agenda) y se agregaron ~65 órdenes de servicio repartidas en los últimos 6 meses con ingresos reales, para que el Dashboard muestre historial completo en vez de "sin datos previos". De paso se corrigió un bug del proxy (`/api/proxy/[...path]`) que rompía cualquier respuesta 204 (afectaba todos los DELETE del sistema, no solo 2FA) |
| 29 | 2026-09-15 | — | v1.60 | Se elimina la solapa Mecánicos por completo — todos los mecánicos son Usuarios. Los 10 mecánicos que ya existían (sin login) se migraron a Usuarios reales en la base de producción (usuario `nombre.apellido`, contraseña provisoria `mecanico2026`). Usuarios ahora muestra nombre/especialidad/estado de cada mecánico y permite editar/activar-desactivar sin pasar por otra pantalla. El rol Mecánico deja de tener una pantalla aislada: usa la sidebar normal pero recortada a solo 2 ítems — Dashboard (una versión restringida, solo sus propios contadores: activas / completadas este mes / vencidas) y Mis Órdenes |
| 28 | 2026-09-14 | `b965680` | v1.59 | Los mecánicos ahora son usuarios de verdad: crear un usuario con rol Mecánico desde Usuarios crea también su perfil de mecánico (Nombre/Teléfono/Especialidad), enlazado por `Mechanic.UserId`. Al loguearse van directo a `/mis-ordenes` — una pantalla propia (sin sidebar de escritorio) que lista sus órdenes activas e historial, filtradas por su nombre asignado. Reforzado también a nivel API: un Mechanic solo puede pegarle a `/serviceorders/mine`, el resto de `ServiceOrdersController` le da 403. `/mecanicos` deja de tener alta manual (ahora se crean desde Usuarios); borrar un mecánico borra su login, y viceversa |
| 27 | 2026-09-14 | `284c3f8` | v1.58 | "Empleados" pasa a llamarse "Usuarios" (`/empleados` → `/usuarios`). Se agrega el rol Admin (mismos permisos que Owner para gestionar usuarios). Al crear un usuario ahora se elige el rol (Admin/Empleado/Mecánico) en vez de crear siempre Empleado. El SuperAdmin y el Dueño ya no se pueden borrar desde acá (antes solo el Dueño estaba protegido) |
| 25 | 2026-09-14 | `136f028` | v1.56 | SuperAdmin pasa a ver toda la app, no solo Configuración: el login lo manda al Dashboard como a cualquiera, `/configuracion` se mueve adentro del layout con sidebar (antes era una página aislada sin manera de volver) y aparece como ítem más en el menú (visible solo para SuperAdmin). Empleados también se habilita para SuperAdmin, no solo Owner |
| 24 | 2026-09-14 | `165686f` | v1.55 | Modelo de un solo taller por sistema: se saca la tab Talleres y toda la gestión multi-taller (crear/activar/desactivar talleres, altas de usuario por-taller) — de acá en más cada cliente que compra el sistema tiene su propio deploy, TallerTotal queda como demo. SuperAdmin pasa a ser un rol real (fila en `Users` con `Role=SuperAdmin`), login unificado sin caso especial — igual que en CEMDI. `/configuracion` queda con 2 tabs: Integraciones y Módulos (ahora edita directo el propio taller, sin selector) |
| 23 | 2026-09-14 | `7d7afed` | v1.54 | `/admin` se fusiona con `/configuracion` (mismo rol SuperAdmin, 3 tabs: Talleres, Integraciones, Módulos). La tab Módulos reemplaza la vieja Configuración por-taller del Owner: ahora el SuperAdmin elige el taller desde un dropdown y edita sus módulos ocultos. El Owner ya no ve Configuración en ningún lado — el endpoint de escritura (`PUT /tenant/modules`) se sacó, solo queda lectura (para filtrar su propio menú) |
| 22 | 2026-09-14 | `dfef28e` | v1.53 | Login unificado: se saca el botón "Acceso administrador" del login — ahora es un único formulario usuario/contraseña, y el SuperAdmin entra con el usuario reservado `superadmin` (antes era un endpoint aparte solo con contraseña). El acceso a `/admin` seguía (y sigue) restringido a rol SuperAdmin vía middleware, invisible para el resto |
| 21 | 2026-09-14 | `a464604` | v1.52 | Se saca Configuración, Apariencia y Cerrar sesión del pie de la sidebar y se arma una topbar nueva (arriba de todo, separada del menú lateral) con esos 3 accesos. Se mueven los toasts de `top-right` a `bottom-right` para que no choquen con la topbar nueva |
| 20 | 2026-09-14 | `aa94f28` | v1.51 | Se agrega este changelog (`BUILDS.md`) y se arranca a bumpear versión en cada deploy |
| 19 | 2026-09-14 13:58 | `3da30f4` | v1.50 | Fix: Informes tiraba 500 al cargar — `top-articulos` agrupaba y sumaba dentro de la misma consulta LINQ tras dos Joins, algo que EF Core no puede traducir a SQL |
| 18 | 2026-09-14 13:48 | `a3c385c` | v1.50 | Fix: la sidebar dejaba ver el fondo blanco de la página por debajo de "Configuración" — al nav le faltaba `overflow-y-auto` y los últimos ítems se desbordaban fuera del panel oscuro |
| 17 | 2026-09-14 13:41 | `47f793f` | v1.50 | Fix: layout angosto en `/configuracion` (usaba `max-w-2xl` en vez de `max-w-5xl` como el resto de la app) |
| 16 | 2026-09-14 13:07-13:28 | `c256db6`…`7b99022` | v1.50 | Renombre de carpeta backend `MecaFlow.Api` → `TallerTotal.Api` y reconexión de auto-deploy en Railway y Vercel (repo transferido a la org Skylia-Dev) |
| 15 | 2026-09-14 12:39 | `c9ad5c1` | v1.50 | Fix: build de producción fallaba — `useSearchParams()` sin `<Suspense>` en `/ventas/nueva` |
| 14 | 2026-09-14 12:18 | `1798ae0` | v1.50 | Ocultamiento de módulos por taller — cada dueño puede desactivar módulos que no usa (migrando concepto de CEMDI) |
| 13 | 2026-09-14 11:08 | `fe43365` | v1.50 | Personalización de tema — modo claro/oscuro + 8 colores de acento |
| 12 | 2026-09-14 11:04 | `493994c` | v1.50 | WhatsApp vía Meta Cloud API además de Evolution API (canal switcheable desde el panel admin) + panel de estado de Email |
| 11 | 2026-09-14 10:03 | `1bb8f1f` | v1.50 | Gestión de empleados con rol dedicado (migrando de FiltCar) |
| 10 | 2026-09-14 09:58 | `17a49bd` | v1.50 | Módulo Auditoría (migrando de FiltCar) |
| 9 | 2026-09-14 09:50 | `c247152` | v1.50 | Módulo Informes (migrando de FiltCar) |
| 8 | 2026-09-14 09:48 | `bada365` | v1.50 | Módulo Caja (migrando de FiltCar) |
| 7 | 2026-09-14 09:45 | `be326c2` | v1.50 | Módulo Presupuestos con conversión a venta (migrando de FiltCar) |
| 6 | 2026-09-14 09:41 | `1a8da18` | v1.50 | Módulos Ventas y Deudas (migrando de FiltCar) |
| 5 | 2026-09-14 09:33 | `85277d7` | v1.50 | Módulo Compras (migrando de FiltCar) |
| 4 | 2026-09-14 09:30 | `1b2e9be` | v1.50 | Módulos Artículos y Proveedores (migrando de FiltCar) |
